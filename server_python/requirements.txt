# Pin chính xác để build lặp lại được (reproducible).
# Nâng version phải chủ động + test lại, không để pip tự lấy bản mới nhất.
fastapi==0.141.1
uvicorn[standard]==0.42.0
pymongo==4.18.0
beanie==2.2.0
python-socketio==5.16.4
python-multipart==0.0.20
PyJWT==2.13.0
bcrypt==5.0.0
python-dotenv==1.2.1
playwright==1.62.0
beautifulsoup4==4.15.0
Unidecode==1.4.0
aiofiles==25.1.0
# Trạng thái dùng chung giữa các tiến trình: blacklist token, rate limit,
# và pub/sub cho Socket.io khi chạy nhiều worker.
redis[hiredis]==6.4.0
# Cho `EmailStr` trong app/schemas/requests.py.
email-validator==2.3.0
# Goi service embedder + dung trong test.
httpx==0.28.1
# Tinh cosine cho ~700 vector: nhanh hon vong lap Python vai chuc lan,
# va du nho de khong can them vector database.
numpy==2.3.5  # ban 2.5.x moi ra, giu ban da kiem chung

# Ma hoa noi dung tin nhan rieng truoc khi ghi xuong Mongo (AES-256-GCM).
# Xem app/services/crypto.py.
cryptography==46.0.3
# Lam sach HTML cua bai viet (chong XSS luu tru). Xem app/utils/sanitize.py.
bleach==6.2.0
